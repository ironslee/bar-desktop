import clientsReducer, {
  setClients,
  selectClient,
  clearClient,
  addClientFromTableOrder,
} from './Clients.slice';
import Clients from './Clients';

export {
  Clients,
  clientsReducer,
  setClients,
  selectClient,
  clearClient,
  addClientFromTableOrder,
};
